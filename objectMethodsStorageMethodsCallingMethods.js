'use strict'

/*
let John = {

    fristName:'John',
    lastName:'Smith',
    mass:92,
    height:1.95,

    bmiCal: function (){
        this.BMI=Math.round(this.mass/(this.height*this.height));
        return this.BMI;
    },

    sumaryJohn: function(){
        this.fullName = `${this.fristName} ${this.lastName}'s BMI (${this.BMI})`;
        return this.fullName;
    }
}

let Mark = {

    fristName:'Mark',
    lastName:'Miller',
    mass: 78,
    height: 1.69,

    bmiCal: function(){
        this.BMI=Math.round(this.mass/(this.height*this.height));
        return this.BMI;
    },

    sumaryMark: function(){
        this.fullName = `${this.fristName} ${this.lastName}'s BMI (${this.BMI})`;
        return this.fullName;
    },
   
}

function calBmi(bmi1,bmi2){
    if(bmi1 > bmi2){
        return `${John.fullName} is higher then ${Mark.fullName}.`;
    }else{
        return `${Mark.fullName} is higher then ${John.fullName}.`;
    }
};

Mark.bmiCal();
John.bmiCal();
Mark.sumaryMark();
John.sumaryJohn();
console.log(calBmi(John.BMI,Mark.BMI));

*/